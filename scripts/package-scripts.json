{
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "migrate:data": "ts-node --project scripts/tsconfig.json scripts/migrate-data.ts",
    "setup:admin": "npm run db:generate && npm run migrate:data"
  }
}